<?php
/**
 * Optionnel : surcharge des identifiants FlexPay (non versionne).
 * Si ce fichier est absent ou les variables vides, `payment_init_11.php`
 * utilise la meme valeur par defaut qu'avant (comportement production inchangé).
 * Pour renforcer la securite, renseignez le JWT ici ou via GNTOMA_FLEXPAY_AUTHORIZATION.
 */
$GNTOMA_FLEXPAY_AUTHORIZATION = '';
$GNTOMA_FLEXPAY_MERCHANT = 'DGB';
